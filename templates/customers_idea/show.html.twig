{% extends 'base.html.twig' %}

{% block title %}CustomersIdea{% endblock %}

{% block body %}
    <h1>CustomersIdea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customers_idea.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ customers_idea.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ customers_idea.lastName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ customers_idea.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ customers_idea.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_customers_idea_index') }}">back to list</a>

    <a href="{{ path('app_customers_idea_edit', {'id': customers_idea.id}) }}">edit</a>

    {{ include('customers_idea/_delete_form.html.twig') }}
{% endblock %}
